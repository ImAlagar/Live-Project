const element = document.getElementById('yourElementId');
if (element) {
  // Access the classList property here
  element.classList.add('some-class');
} else {
  console.error("Element not found");
}
